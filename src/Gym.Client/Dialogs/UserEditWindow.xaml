<Window x:Class="Gym.Client.Dialogs.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User" Height="250" Width="350">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding Username}" Grid.Row="0" Grid.Column="1" Margin="5"/>
        <TextBlock Text="Role:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <ComboBox x:Name="RoleBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <TextBlock Text="Email:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding Email}" Grid.Row="2" Grid.Column="1" Margin="5"/>
        <CheckBox Content="Enabled" IsChecked="{Binding IsEnabled}" Grid.Row="3" Grid.Column="1" Margin="5"/>
        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.ColumnSpan="2" Visibility="Collapsed" x:Name="PwdPanel">
            <TextBlock Text="Password:" Margin="5"/>
            <PasswordBox x:Name="PwdBox" Width="150" Margin="5"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="5" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
